extends layout

block content
  
  h1= title
  p This is web apps assignment 10. Working on these assignments is interesting.
  div(class="container")
    button(Type="button", class="btn btn-secondary btn-lg" onclick="update()") Press me
    div(id="target") Display


  script. 
      var client_count=0 
      function update(){
        client_count++;
        document.getElementById("target").innerHTML=`Value now ${client_count}`
      }